import 'package:flutter/material.dart';

class DraftItemsScreen extends StatelessWidget {
  const DraftItemsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
